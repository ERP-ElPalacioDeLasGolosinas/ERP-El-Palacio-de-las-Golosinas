import { PlaceholderModule } from "@/components/layout/PlaceholderModule";

export const metadata = {
  title: "Unidades de medida | Palacio · ERP",
};

export default function UnidadesMedidaPage() {
  return (
    <PlaceholderModule
      crumbs={[{ label: "Unidades de medida" }]}
      title="Unidades de medida"
      description="Gestión de unidades de medida del catálogo. Pendiente de implementación (A-01)."
    />
  );
}
